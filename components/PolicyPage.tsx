import SiteHeader from "./SiteHeader";import SiteFooter from "./SiteFooter";
export default function PolicyPage({eyebrow,title,intro,sections}:{eyebrow:string;title:string;intro:string;sections:{heading:string;body:string}[]}){return <main><SiteHeader/><section className="policy-layout"><aside><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{intro}</p><div className="policy-note">Draft website copy for structural review. Final wording must be reviewed against your actual operating policies and legal requirements before launch.</div></aside><article>{sections.map(s=><section key={s.heading}><h2>{s.heading}</h2><p>{s.body}</p></section>)}</article></section><SiteFooter/></main>}
