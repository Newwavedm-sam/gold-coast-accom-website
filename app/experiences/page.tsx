import SiteHeader from "../../components/SiteHeader";
import { experiences } from "../../lib/discovery-data";
export default function ExperiencesPage(){return <main><SiteHeader/><section className="editorial-hero experiences-hero"><p className="script teal">Play. Stay. Connect.</p><h1>Let the coast<br/>surprise you.</h1><p>Distinctive local experiences arranged around your stay. Every experience begins with a conversation, so we can shape it to your group.</p></section><section className="experience-index-grid">{experiences.map((item,index)=><a href={`/experiences/${item.slug}`} className="experience-index-card" key={item.slug} style={{backgroundImage:`linear-gradient(180deg,transparent 30%,rgba(5,48,67,.82)),url(${item.image})`}}><span>0{index+1} · {item.category}</span><div><h2>{item.name}</h2><p>{item.tagline}</p><strong>Discover more →</strong></div></a>)}</section><section className="concierge-band"><p className="eyebrow">Something else in mind?</p><h2>Let our local team bring it together.</h2><a className="button secondary" href="/concierge">Ask the concierge</a></section></main>}
